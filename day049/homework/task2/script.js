const pi = 3.14;
const radius = 10;

document.getElementById('area').innerText = `Circle Area: ${pi * (radius * radius)}`;
document.getElementById('circumference').innerText = `Circle Circumference: ${2 * pi * radius}`;